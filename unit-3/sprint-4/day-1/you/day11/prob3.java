package day11;

public class prob3 {
   /*
   * Q3) Explain the difference between abstract class and interface.
   * Abstract classes can have abstract as well as non-abstract methods while interface can have only abstract methods.
   * Abstract classes doesn't support multiple imheritance but interface does.
   * An abstract class can implement interface but interface doesn't.
   * Abstract classes can provide abstraction in between 0 to 100% but interface can provide only 100% abstraction.
   *
   *
   *
   *
   * */
}
