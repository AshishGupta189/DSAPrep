package day_1;

public class p1 {
	
	/*
	 * What is the Difference between Comparable and Comparator? Explain with
	 * examples. 
	 * Comparable belongs to java.lang package while Comparator belong to jva.util package.
	 * comparable uses compareTo method and takes only one parameter while Comparator uses compare method
	 * and takes 2 parameter
	 * by comparable we can only sort by one parameter while with comparator we can sort using many parameters.
	 * Comparable is used inside the class while comparator is used outside the class.
	 *
	 */}
