package Assignments;

public class Prob1 {
	/*
	 * //Q1/- What is the difference between Statment and PreparedStatement?
	 * 
	 * Statements are faster while Prepared Statements are slower than Statements.
	 * If we want to execute the query once , we should go with Statements.
	 * PreparedStatments optimizes query and solver 3 different problems such as SQL injection.
	 * 
	 */
	
}
